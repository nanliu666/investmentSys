/**
 * lodash按需引入
 */
import _ from 'lodash'
export default {
  cloneDeep: _.cloneDeep,
  debounce: _.debounce,
  throttle: _.throttle,
  size: _.size,
  pick: _.pick,
  pickBy: _.pickBy,
  isEmpty: _.isEmpty,
  groupBy: _.groupBy,
  filter: _.filter,
  chain: _.chain,
  uniqBy: _.uniqBy,
  find: _.find,
  map: _.map,
  slice: _.slice,
  orderBy: _.orderBy,
  forEach: _.forEach
}
